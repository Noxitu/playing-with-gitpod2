name: 'Hello World'
description: 'My Action Desc'
inputs:
  arg:
    description: 'arg desc'
    required: true
    default: 'World'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.arg }}
    - World
