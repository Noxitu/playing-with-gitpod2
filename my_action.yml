name: 'Hello World'
description: 'My Action Desc'
inputs:
  my-param2:
    description: 'arg desc'
    required: true
    default: 'World'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.my-param3 }}
