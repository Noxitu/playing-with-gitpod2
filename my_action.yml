name: 'Hello World'
description: 'My Action Desc'
inputs:
  actionArgument:
    description: 'arg desc'
    required: true
    default: 'World'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.actionArgument }}
